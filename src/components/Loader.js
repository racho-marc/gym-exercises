import React from 'react';
import { LoaderOverlay, LoaderContainer } from './Loader.styles';

const Loader = () => {
  return (
    <LoaderOverlay>
      <LoaderContainer />
    </LoaderOverlay>
  )
}

export default Loader;